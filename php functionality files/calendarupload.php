<?php
/*
Specifications:

Calendar Update System

This is the Calendar modification/update system, which allows the administrator to add, delete, and
update events from the Calendar database. 

$mysqli = new mysqli("");
if ($mysqli->errno) {
	print($mysqli->error);
	exit();
}

if(isset(Add button)){
	$query = insert query
	$result = $mysqli->query($query);
}

if(isset(Delete button)){
	$query = delete query
	$result = $mysqli->query($query);
}

if(isset(Update button)){
	$query = update query
	$result = $mysqli->query($query);

}

*/
?>